boto3>=1.34
botocore>=1.34
pytest>=8.0

# K8s stretch goal only (scanner.k8s_cli / scanner.k8s_engine) — not used
# by the AWS pipeline or the deployed Lambda, whose zip only bundles src/
# and relies on boto3 shipping with the Lambda runtime already.
kubernetes>=31.0
